services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: gestor_db
      POSTGRES_PASSWORD: tupassword
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - "8000:8000"
    depends_on:
      - db